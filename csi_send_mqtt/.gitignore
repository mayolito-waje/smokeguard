# Build configuration is regenerated by `idf.py set-target`; the tracked copy is
# sdkconfig.defaults. (The repo root already covers build/ and sdkconfig.old.)
sdkconfig

# Generated from csi_recv_mqtt/.env at configure time. It contains the hotspot
# password, so like that .env it must never be committed.
main/wifi_config.h
